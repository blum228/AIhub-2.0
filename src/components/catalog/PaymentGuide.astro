---
interface Props {
  acceptsRussianCards: boolean;
  paymentMethods?: string[];
}

const { acceptsRussianCards, paymentMethods = [] } = Astro.props;

const methodLabels: Record<string, string> = {
  card: 'Visa/Mastercard (иностранные)',
  crypto: 'Криптовалюта',
  paypal: 'PayPal',
  qiwi: 'Qiwi',
  yoomoney: 'ЮMoney'
};
---

{!acceptsRussianCards && (
  <aside class="payment-guide">
    <h4 class="payment-guide__title">💳 Как оплатить из России</h4>
    <p class="payment-guide__text">
      Этот сервис не принимает карты российских банков напрямую. Вот альтернативные способы оплаты:
    </p>

    {paymentMethods.length > 0 && (
      <div class="payment-guide__methods">
        <strong>Доступные способы:</strong>
        <ul>
          {paymentMethods.map(method => (
            <li>{methodLabels[method] || method}</li>
          ))}
        </ul>
      </div>
    )}

    <div class="payment-guide__tips">
      <strong>Рекомендации:</strong>
      <ul>
        <li>Используйте криптовалюту (USDT, BTC) через биржи</li>
        <li>Оформите виртуальную карту через сервисы типа Pyypl, Wise</li>
        <li>Попросите друзей за рубежом помочь с оплатой</li>
      </ul>
    </div>
  </aside>
)}

<style>
  .payment-guide {
    background: rgba(var(--ids__accent-RGB), 0.05);
    border: 1px solid rgba(var(--ids__accent-RGB), 0.2);
    border-radius: var(--ids__radius);
    padding: 1.2em 1.5em;
    margin: 1.5em 0;
  }

  .payment-guide__title {
    margin: 0 0 0.5em;
    font-size: 1.1em;
  }

  .payment-guide__text {
    margin: 0 0 1em;
    opacity: 0.9;
  }

  .payment-guide__methods,
  .payment-guide__tips {
    margin-bottom: 1em;
  }

  .payment-guide__methods:last-child,
  .payment-guide__tips:last-child {
    margin-bottom: 0;
  }

  .payment-guide ul {
    margin: 0.5em 0 0;
    padding-left: 1.2em;
  }

  .payment-guide li {
    margin-bottom: 0.3em;
  }
</style>
