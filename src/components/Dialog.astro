---
/**
 * Контейнер для диалога с репликами
 * Группирует DialogLine компоненты с отступами
 */
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["dialog", className]}>
  <slot />
</div>
