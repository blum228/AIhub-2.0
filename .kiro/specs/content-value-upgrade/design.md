# Design Document: Content Value Upgrade

## Overview

Апгрейд контента агрегатора AI-девушек для превращения сырого каталога в полезный продукт, помогающий пользователям выбрать подходящий сервис. Изменения затрагивают только контент и смыслы — дизайн компонентов остаётся без изменений (IDS Full).

Основной принцип: каждый элемент сайта должен помогать пользователю принять решение о выборе сервиса.

## Architecture

### Текущая структура контента
```
src/content/
├── tools/           # 15 сервисов (включая нерелевантные)
├── collections/     # 5 коллекций (слабый контент)
└── blog/           # 4 статьи
```

### Целевая структура контента
```
src/content/
├── tools/           # 10-12 релевантных сервисов (AI-девушки + ролеплей)
├── collections/     # 5 коллекций с улучшенным контентом
└── blog/           # 4 статьи (переработанные)
```

### Изменения в страницах
- `index.astro` — новый hero-контент с ценностным предложением
- `catalog/index.astro` — добавление сценариев быстрого выбора
- `tool/[slug].astro` — улучшение контента (без изменения компонентов)
- `category/[slug].astro` — добавление сравнительных таблиц в контент коллекций

## Components and Interfaces

### Изменения в контенте (не в компонентах)

#### 1. Hero Section Content
Текущий контент:
```
headline: "AI Девушки — Рейтинг 2024"
description: "Лучшие сервисы для общения с виртуальными подругами..."
```

Новый контент:
```
headline: "Найди свою AI-девушку"
description: "Помогаем выбрать сервис для общения с AI. Честные сравнения, фильтры по доступности из России, рекомендации под твой сценарий."
```

#### 2. Quick Scenarios Block (новый контент на главной)
Три карточки сценариев:
- "Без VPN" → фильтр requiresVpn: false
- "Карты РФ" → фильтр acceptsRussianCards: true  
- "Бесплатно" → фильтр priceModel: free

#### 3. Tool Page Content Structure
Каждый MDX-файл сервиса должен содержать:
```markdown
## О сервисе
[Краткое описание]

## Для кого подходит
- Сценарий 1
- Сценарий 2
- Сценарий 3

## Ключевые возможности
[Список фич]

## Плюсы и минусы
**Плюсы:**
- ...

**Минусы:**
- ...

## Тарифы
[Таблица с ценами в $ и примерно в ₽]

## Как начать
[Пошаговая инструкция]

## Альтернативы
[Сравнение с 2-3 похожими сервисами]
```

#### 4. Collection Content Structure
Каждый MDX-файл коллекции должен содержать:
```markdown
## [Название категории]
[Вводный текст: для кого эта подборка]

### 🏆 Лучший выбор: [Название сервиса]
[Почему рекомендуем именно его]

### Сравнительная таблица
| Сервис | Качество | Цензура | Карты РФ | VPN | Цена |
...

### На что обратить внимание
[Критерии выбора для этой категории]
```

## Data Models

### Существующая схема ToolData (без изменений)
```typescript
interface ToolData {
  slug: string;
  title: string;
  shortDescription: string;
  coverImage: string;
  tags: string[];
  priceModel: 'free' | 'freemium' | 'paid';
  priceFrom?: number;
  isNsfw: boolean;
  affiliateLink: string;
  rating?: number;
  acceptsRussianCards: boolean;
  requiresVpn: boolean;
  supportsRussian: boolean;
  paymentMethods?: string[];
  faq?: { question: string; answer: string }[];
  publishedAt: Date;
  updatedAt?: Date;
  collections?: string[];
}
```

### Рекомендуемое добавление поля updatedAt
Для отображения актуальности информации использовать существующее поле `updatedAt`.

## Correctness Properties

*A property is a characteristic or behavior that should hold true across all valid executions of a system-essentially, a formal statement about what the system should do. Properties serve as the bridge between human-readable specifications and machine-verifiable correctness guarantees.*

### Prework Analysis

1.1 WHEN пользователь открывает главную страницу THEN система SHALL отображать заголовок, чётко объясняющий ценность сервиса
Thoughts: Это проверка наличия конкретного контента на странице. Можно проверить что hero содержит ключевые слова ценности.
Testable: yes - example

1.2 WHEN пользователь видит hero-секцию THEN система SHALL показывать 3 ключевых преимущества каталога
Thoughts: Проверка наличия трёх элементов преимуществ. Можно проверить структуру.
Testable: yes - example

1.3 WHEN пользователь просматривает главную THEN система SHALL отображать быстрые ссылки на ключевые сценарии
Thoughts: Проверка наличия ссылок на сценарии. Конкретный пример.
Testable: yes - example

2.1 WHEN пользователь ищет сервис без VPN THEN система SHALL показывать отфильтрованный список
Thoughts: Это свойство фильтрации — для любого набора сервисов, фильтр по VPN должен возвращать только сервисы с requiresVpn: false
Testable: yes - property

2.2 WHEN пользователь ищет сервис с оплатой картой РФ THEN система SHALL показывать только сервисы, принимающие российские карты
Thoughts: Аналогично — свойство фильтрации по acceptsRussianCards
Testable: yes - property

2.3 WHEN пользователь хочет бесплатный сервис THEN система SHALL показывать сервисы с бесплатным тарифом
Thoughts: Свойство фильтрации по priceModel
Testable: yes - property

3.1 WHEN пользователь открывает страницу сервиса THEN система SHALL отображать блок "Для кого подходит"
Thoughts: Проверка структуры контента MDX. Можно проверить наличие секции.
Testable: yes - example

3.2 WHEN пользователь читает описание THEN система SHALL показывать честные плюсы и минусы
Thoughts: Проверка наличия секций плюсов и минусов в контенте.
Testable: yes - example

3.3 WHEN сервис требует VPN THEN система SHALL показывать инструкции по обходу
Thoughts: Для всех сервисов с requiresVpn: true должна быть инструкция. Это свойство.
Testable: yes - property

3.4 WHEN пользователь просматривает тарифы THEN система SHALL показывать стоимость
Thoughts: Проверка наличия таблицы тарифов в контенте.
Testable: yes - example

4.1 WHEN пользователь на странице сервиса THEN система SHALL показывать сравнение с альтернативами
Thoughts: Проверка наличия секции альтернатив в контенте.
Testable: yes - example

4.2 WHEN пользователь просматривает коллекцию THEN система SHALL отображать сравнительную таблицу
Thoughts: Проверка наличия таблицы в контенте коллекции.
Testable: yes - example

5.1 WHEN пользователь открывает коллекцию THEN система SHALL показывать вводный текст
Thoughts: Проверка наличия вводного текста в коллекции.
Testable: yes - example

5.2 WHEN пользователь просматривает коллекцию THEN система SHALL отображать рекомендацию "Лучший выбор"
Thoughts: Проверка наличия рекомендации в контенте коллекции.
Testable: yes - example

6.1 WHEN пользователь использует фильтры THEN система SHALL показывать количество результатов
Thoughts: Это UI-поведение, требует проверки рендеринга.
Testable: no

6.2 WHEN пользователь применяет несколько фильтров THEN система SHALL комбинировать их логически
Thoughts: Свойство комбинирования фильтров — для любых двух фильтров результат должен удовлетворять обоим.
Testable: yes - property

7.1 WHEN пользователь просматривает сервис THEN система SHALL показывать дату обновления
Thoughts: Проверка наличия даты в данных сервиса.
Testable: yes - example

8.1 WHEN сервис не относится к AI-девушкам THEN система SHALL удалить или переместить его
Thoughts: Это ручная проверка релевантности контента.
Testable: no

### Property Reflection

Анализ избыточности:
- Свойства 2.1, 2.2, 2.3 — все про фильтрацию, можно объединить в одно общее свойство фильтрации
- Свойство 6.2 — частный случай комбинирования фильтров, можно объединить с 2.x
- Свойство 3.3 — уникальное, проверяет связь данных и контента

Итоговые свойства:
1. Фильтрация по критериям (объединяет 2.1, 2.2, 2.3, 6.2)
2. Инструкции для ограниченных сервисов (3.3)

### Correctness Properties

Property 1: Filter consistency
*For any* set of tools and any filter criterion (requiresVpn, acceptsRussianCards, priceModel), applying the filter SHALL return only tools that satisfy the criterion, and combining multiple filters SHALL return only tools satisfying ALL criteria.
**Validates: Requirements 2.1, 2.2, 2.3, 6.2**

Property 2: VPN instruction presence
*For any* tool with requiresVpn: true, the tool's MDX content SHALL contain instructions for accessing the service (VPN setup or alternatives).
**Validates: Requirements 3.3**

## Error Handling

### Контентные ошибки
- Отсутствие обязательных секций в MDX → валидация при сборке
- Устаревшие данные (updatedAt > 3 месяцев) → визуальное предупреждение
- Битые ссылки на сервисы → проверка при сборке

### Пользовательские сценарии
- Пустые результаты фильтрации → показать альтернативные варианты
- Нерелевантный поиск → предложить популярные категории

## Testing Strategy

### Unit Tests
- Проверка функций фильтрации в `src/lib/tools.ts`
- Валидация структуры MDX-контента

### Property-Based Tests
Используем библиотеку `fast-check` для TypeScript:

1. **Filter consistency test**: Генерируем случайные наборы инструментов и фильтров, проверяем что результат соответствует критериям
2. **VPN instruction test**: Для всех инструментов с requiresVpn: true проверяем наличие инструкций в контенте

### Content Validation
- Проверка наличия обязательных секций в MDX при сборке
- Линтинг контента на соответствие структуре

